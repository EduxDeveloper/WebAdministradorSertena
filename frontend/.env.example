# URL de la API. Comenta la que NO uses y deja activa solo una.
# Producción (Render)
# VITE_API_URL=https://sertenaapi.onrender.com/api

# Desarrollo local (usa el proxy de Vite → localhost:4000)
VITE_API_URL=/api
